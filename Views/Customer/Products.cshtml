@model IEnumerable<Furniture.Models.ProductProject>

@{
      Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Products</title>
</head>
<body>
    @if(Model.Any()){
          <div class="text-center container py-5">
                    <div class="row" data-aos="fade-right">
            @foreach (var item in Model){
                         <div class="col-lg-4 col-md-12 mb-4">
                    <div class="card">
                      <div class="bg-image hover-zoom ripple ripple-surface ripple-surface-light"
            data-mdb-ripple-color="light">
                            <img src=@Url.Content("~/image/" + item.ImagePath) alt=""  asp-append-version="true" width="200" height="300" class="w-100" />
                        <a href="#!">
                          <div class="mask">
                            <div class="d-flex justify-content-start align-items-end h-100">
                              <h5><span class="badge bg-primary ms-2">New</span></h5>
                            </div>
                          </div>
                          <div class="hover-overlay">
                            <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                          </div>
                        </a>
                      </div>
                      <div class="card-body">
                        <a href="" class="text-reset">
                          <h5 class="card-title mb-3">@item.ProductName</h5>
                        </a>
                        <a href="" class="text-reset">
                              <p>@item.Category.Categoryname</p>
                        </a>
                            <h6 class="mb-3">$@item.Price</h6>
                                  <a asp-action="Shopping" asp-controller="Customer" asp-route-id="@item.Id" class="btn mb-1 btn-primary">Add to cart <i class="bi bi-cart4"></i></a>
                      </div>
                    </div>
                  </div>
            }
                    
                    </div>
                    </div>
}

else {
       <div class="alert alert-primary" role="alert" style="padding:30px">
              <center>There are no products now</center>
</div>
}
</body>
</html>


@* ------------------------------------------------------------------*@

