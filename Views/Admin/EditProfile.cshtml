@model Furniture.Models.UserAccount

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var username = ViewBag.username;
    var password = ViewBag.password;
}



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ShowProfile</title>

</head>
<body>

    <div class="container px-4 mt-4">

    <div class="row">
               
        <div class="col-xl-8">
            <!-- Account details card-->
            <div class="card mb-4">
                <div class="card-header">Account Details</div>
                <div class="card-body">
                    <form asp-action="EditProfile" enctype="multipart/form-data">
                        <input asp-for="Id" hidden />
                        <!-- Form Group (username)-->
                        <div class="mb-3">
                            <label asp-for="Fullname" class="control-label" ></label>
                            <input asp-for="Fullname" class="form-control"  />
                        </div>
                        <!-- Form Row-->
                        <div class="row gx-3 mb-3">
                            <!-- Form Group (first name)-->
                            <div class="col-md-6">
                                 <label asp-for="Email"  class="control-label"></label>
                                 <input asp-for="Email"class="form-control"  />
                            </div>
                            <!-- Form Group (last name)-->
                            <div class="col-md-6">
                                  <label asp-for="Phone"  class="control-label"></label>
                                  <input asp-for="Phone"class="form-control" />
                            </div>
                        </div>
                        <!-- Form Row        -->
                        <div class="row gx-3 mb-3">
                            <!-- Form Group (organization name)-->
                            <div class="col-md-6">
                               <label  class="control-label">User name</label>
                                  <input type="text" class="form-control"   value="@Model.Logins.SingleOrDefault().Username" name="username" id="username"/>
                            </div>
                            <!-- Form Group (location)-->
                            <div class="col-md-6">
                               <label  class="control-label">Password</label>
                                  <input  class="form-control" value="@Model.Logins.SingleOrDefault().Passwordd" type="password"  name="password" id="password"/>
                            </div>
                        </div>
                             <div class="row gx-3 mb-3">
                          <div class="col-md-6">
                              <label asp-for="ImageFile" class="control-label"></label>
                <input asp-for="ImageFile" class="form-control" />
                <span asp-validation-for="ImageFile" class="text-danger"></span>
            </div>
       
            </div>

                        <!-- Save changes button-->
                          <input type="submit" class="btn btn-primary">Edit Your Information</a> 
                          <a asp-action="ShowProfile" class="btn btn-primary">Back</a> 

                          </form>


                </div>
            </div>
        </div>
    </div>
</div>

 </body>
 </html>
